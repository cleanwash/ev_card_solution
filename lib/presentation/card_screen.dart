import 'package:flutter/material.dart';

class CardSceen extends StatelessWidget {
  const CardSceen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}